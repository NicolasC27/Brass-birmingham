/* Where the guide keeps what the reader arranged: the note's fold and its
 * place on the screen. Their own file, so the launcher of a guided game can
 * clear them without pulling the whole panel in. */

/** the lesson the note is folded on, or -1 */
export const MINI_KEY = 'brassworks.guide.mini';
/** the spot the note was dragged to */
export const POS_KEY = 'brassworks.guide.pos';
